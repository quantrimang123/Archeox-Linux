#!/bin/bash

# Archeox:summary=Launch a TUI command in the default terminal with Archeox styling
# Archeox:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.Archeox.$(basename $1) -e "$1" "${@:2}"
