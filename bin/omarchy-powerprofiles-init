#!/bin/bash

# ARCHEOX:summary=Set the correct power profile on boot based on current AC/battery state.

if ARCHEOX-battery-present && ! ARCHEOX-ac-present; then
  ARCHEOX-powerprofiles-set battery
else
  ARCHEOX-powerprofiles-set ac
fi
