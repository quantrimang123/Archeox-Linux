#!/bin/bash

# ILV:summary=Set the correct power profile on boot based on current AC/battery state.

if ILV-battery-present && ! ILV-ac-present; then
  ILV-powerprofiles-set battery
else
  ILV-powerprofiles-set ac
fi
