#!/bin/bash

# ARCHEOX:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the ARCHEOX defaults and restart the service.

ARCHEOX-refresh-config swayosd/config.toml
ARCHEOX-refresh-config swayosd/style.css
ARCHEOX-restart-swayosd
