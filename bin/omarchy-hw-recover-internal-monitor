#!/bin/bash

# ILV:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/ILV/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! ILV-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
