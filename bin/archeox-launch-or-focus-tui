#!/bin/bash

# ARCHEOX:summary=Launch a TUI or focus an existing terminal window for it
# ARCHEOX:args=<command> [args...]

APP_ID="org.ARCHEOX.$(basename "$1")"
LAUNCH_COMMAND="ARCHEOX-launch-tui $@"

exec ARCHEOX-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
