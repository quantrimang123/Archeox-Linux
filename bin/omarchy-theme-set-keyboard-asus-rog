#!/bin/bash

# ILV:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# ILV:hidden=true

ASUSCTL_THEME=~/.config/ILV/current/theme/keyboard.rgb

if ILV-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
