#!/bin/bash

# ARCHEOX:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/ARCHEOX/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! ARCHEOX-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
