#!/bin/bash

# ARCHEOX:summary=Restart an application by killing it and relaunching via uwsm.
# ARCHEOX:args=<application-name> [application-args...]

pkill -x $1
setsid uwsm-app -- "$@" >/dev/null 2>&1 &
