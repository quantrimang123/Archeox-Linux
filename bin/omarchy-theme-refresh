#!/bin/bash

# ARCHEOX:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/ARCHEOX/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ARCHEOX_THEME_SKIP_BACKGROUND=1 ARCHEOX-theme-set "$(cat $THEME_NAME_PATH)"
fi
