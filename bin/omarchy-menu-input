#!/bin/bash

# ILV:summary=Prompt for text input with Walker
# ILV:group=menu
# ILV:name=input
# ILV:args=prompt [walker args...]
# ILV:examples=ILV menu input Reminder|ILV-menu-input "Reminder in minutes" --width 400

set -euo pipefail

prompt="${1:-Input}"
if (( $# > 0 )); then
  shift
fi

ILV-launch-walker --dmenu --inputonly --width 295 --minheight 1 --maxheight 1 -p "$prompt…" "$@" 2>/dev/null
