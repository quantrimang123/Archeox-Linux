#!/bin/bash

# archeox:summary=Toggle Waybar visibility
# archeox:examples=archeox toggle waybar

archeox-toggle waybar-off

if pgrep -x waybar >/dev/null; then
  pkill -9 -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
