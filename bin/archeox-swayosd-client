#!/bin/bash

# Archeox:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# Archeox:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(Archeox-hyprland-monitor-focused)" "$@"
