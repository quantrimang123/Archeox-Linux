#!/bin/bash

# archeox:summary=List available monospace fonts
# archeox:examples=archeox font list | archeox font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|archeox' | sort -u
