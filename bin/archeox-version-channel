#!/bin/bash

# Archeox:summary=Print the active Archeox mirror and package channel

if grep -q "https://stable-mirror.Archeox.org/" /etc/pacman.d/mirrorlist; then
  mirror="stable"
elif grep -q "https://rc-mirror.Archeox.org/" /etc/pacman.d/mirrorlist; then
  mirror="rc"
elif grep -q "https://mirror.Archeox.org/" /etc/pacman.d/mirrorlist; then
  mirror="edge"
else
  mirror="unknown"
fi

if grep -q "https://pkgs.Archeox.org/stable/" /etc/pacman.conf; then
  pkgs="stable"
elif grep -q "https://pkgs.Archeox.org/edge/" /etc/pacman.conf; then
  pkgs="edge"
elif grep -q "https://pkgs.Archeox.org/rc/" /etc/pacman.conf; then
  pkgs="rc"
else
  pkgs="unknown"
fi

if [[ $mirror == $pkgs ]]; then
  echo $mirror
else
  echo "$mirror / $pkgs"
fi
