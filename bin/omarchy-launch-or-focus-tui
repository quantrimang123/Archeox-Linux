#!/bin/bash

# ILV:summary=Launch a TUI or focus an existing terminal window for it
# ILV:args=<command> [args...]

APP_ID="org.ILV.$(basename "$1")"
LAUNCH_COMMAND="ILV-launch-tui $@"

exec ILV-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
