#!/bin/bash

# archeox:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# archeox:hidden=true

ASUSCTL_THEME=~/.config/archeox/current/theme/keyboard.rgb

if archeox-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
