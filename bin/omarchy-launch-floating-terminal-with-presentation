#!/bin/bash

# ILV:summary=Launch a floating terminal with the Omarchy presentation wrapper
# ILV:args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.ILV.terminal --title=Omarchy -e bash -c "ILV-show-logo; $cmd; if (( \$? != 130 )); then ILV-show-done; fi"
