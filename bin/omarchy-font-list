#!/bin/bash

# ILV:summary=List available monospace fonts
# ILV:examples=ILV font list | ILV font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|ILV' | sort -u
