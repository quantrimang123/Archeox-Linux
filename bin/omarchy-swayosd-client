#!/bin/bash

# ILV:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# ILV:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(ILV-hyprland-monitor-focused)" "$@"
