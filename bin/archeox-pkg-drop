#!/bin/bash

# ARCHEOX:summary=Remove all the named packages from the system if they're installed (otherwise ignore).
# ARCHEOX:args=<packages...>
# ARCHEOX:requires-sudo=true

installed=()
for pkg in "$@"; do
  if pacman -Qq | grep -Fxq "$pkg"; then
    installed+=("$pkg")
  fi
done

if (( ${#installed[@]} > 0 )); then
  sudo pacman -Rns --noconfirm "${installed[@]}"
fi
